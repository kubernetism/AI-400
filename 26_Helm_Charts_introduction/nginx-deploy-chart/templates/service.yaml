apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-deploy-chart.fullname" . }}
  labels:
    {{- include "nginx-deploy-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-deploy-chart.selectorLabels" . | nindent 4 }}
