apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: todo-manager
  labels:
    app.kubernetes.io/part-of: agentic-todo-manager
type: Opaque
stringData:
  # ── Database ──
  DATABASE_URL: "postgresql://neondb_owner:<PASSWORD>@ep-muddy-flower-a4ck543x-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

  # ── LLM API Keys ──
  OPENAI_API_KEY: "<YOUR_OPENAI_API_KEY>"
