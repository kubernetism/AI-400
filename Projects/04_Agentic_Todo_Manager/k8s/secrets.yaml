apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: todo-manager
  labels:
    app.kubernetes.io/part-of: agentic-todo-manager
type: Opaque
stringData:
  # ── Database ──
  DATABASE_URL: "postgresql://postgres:password@postgres:5432/todo_manager_db"

  # ── LLM API Keys ──
  OPENAI_API_KEY: "sk-proj-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"
